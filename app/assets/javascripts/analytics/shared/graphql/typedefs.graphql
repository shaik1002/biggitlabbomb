type FlowMetricsCommits {
  unit: String!
  value: Int!
  identifier: String!
  links: [String] # TODO: expand on this since its a nested obj
  title: String!
}

extend type Query {
  flowMetricsCommits(
    fullPath: ID!
    from: Time!
    to: Time!
    labelNames: [String!]
  ): FlowMetricsCommits
}
